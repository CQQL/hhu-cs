apply plugin: "java"
apply plugin: "application"

sourceCompatibility = 1.8
version = "1.0"

mainClassName = "WebServer"
run.args = ["-mime", "mime.types", "-path", "."]

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: "junit", name: "junit", version: "4.11"
}
